#pragma once
#include <string>

class ITacticalCommandInterface {
public:
    virtual ~ITacticalCommandInterface() = default;
    // Define pure virtual functions as needed, using std::string, etc.
}; 